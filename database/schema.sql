CREATE TABLE users (username text,
                    usertag text,
                    userid text);

CREATE TABLE guilds (
            id int,
            prefix text,
            autorole int,
            afk_channel int,
            logs_channel int,
);

